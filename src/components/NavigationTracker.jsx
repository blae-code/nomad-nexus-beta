/**
 * Navigation Tracker
 * Tracks route changes and manages page navigation context
 */
import React from 'react';

// Shim: prevent any accidental imports from stale paths
// The actual pages config is at @/components/pages/config

export default function NavigationTracker() {
  return null;
}