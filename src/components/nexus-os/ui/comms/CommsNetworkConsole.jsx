/**
 * CommsNetworkConsole (JSX canonical entrypoint)
 * TSX implementation is retained for compatibility while migration remains in progress.
 */
export { default } from './CommsNetworkConsole.tsx';
