# LiveKit (server-side) credentials
# Use secure values in production and never commit real secrets.
LIVEKIT_URL=https://your-livekit-host
LIVEKIT_API_KEY=your_livekit_api_key
LIVEKIT_API_SECRET=your_livekit_api_secret

# Base44 (client-side Vite config)
# These values are safe to expose in the client, but should still point to the
# correct Base44 app and backend for your environment.
VITE_BASE44_APP_ID=your_base44_app_id
VITE_BASE44_BACKEND_URL=https://your-base44-backend.example
